import * as React from "react";

// - Claim
// Add project and task --> show up on home page (with a new flag), read about the project

export default () => (
  <div>
    Welcome to the Kinspire Volunteer Hub. Check out relevant links at the top
    of the page.
  </div>
);
